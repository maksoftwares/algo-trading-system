# Causal Health-Gated Event Sleeves V100

Final preregistered attempt to add enough XAUUSD frequency to frozen V59/V60.
The campaign schedules deterministic V19 event episodes and permits entries
only while their strictly prior completed markouts satisfy a rolling health
threshold.

Run `lock_contract.py`, then open `discovery`, `confirmation`, and `final` with
`run_research.py` only when the preceding advancement lock names a survivor.
Run `run_shared_audit.py` only after a Final survivor exists.
