# V60 Dynamic Health-Margin Union V8 Preregistration

V8 is the final bounded health-margin test. V6 used PF `<1.00` and failed only the
2021 annual gate at +$0.20 cost. V7 used PF `<0.90`, reduced the +$0.10 veto cohort
below 10, and worsened stressed drawdown. V8 freezes the midpoint PF `<0.95`.

No other policy, feature, cost, data, or acceptance gate changes. State is recomputed
causally in every scenario using the V6 dynamic runner. All outcomes are exposed, so
even a complete retrospective pass can only nominate read-only forward observation.

This lane will not screen another health-margin threshold after V8.

Broker action, runtime changes, demo deployment, and live deployment are prohibited.
