# Causal Canonical Profit Policy V12

Profit-focused research policy over the frozen V10 expected-R model.

Run in order:

```powershell
python lock_contract.py
python run_evaluation.py
python verify.py
python -m pytest -q
```

This package is offline research only. It does not modify MT5, runtime
authorization, shadow scoring, or execution.
