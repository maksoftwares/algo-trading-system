# XAUUSD Multi-Regime Review Evidence

This directory closes the independent-review gap without changing strategy logic, thresholds, data selection, execution assumptions, costs or economic results.

- `A1_XAU_MULTIREGIME_BASE_RESEARCH.bundle` contains the exact base and research commits and declares the base parent as its sole prerequisite.
- `A1_XAU_MULTIREGIME_BASE_TO_RESEARCH.patch` is the full binary diff from the base commit to the research commit, restricted to the authorized lane.
- `RESEARCH_COMMIT_IDENTITY.json` records exact commit, tree and parent identities.
- `CLEAN_WORKTREE_EVIDENCE.txt` records the empty status of a detached checkout of the research commit.
- `FRESH_CHECKOUT_BYTE_NORMALIZATION_FINDING.txt` discloses and explains the original Windows checkout-byte issue.
- `ORIGINAL_COMMIT_DETERMINISTIC_REPLAY.json` records both hash sets for the 14 original outputs.
- `ORIGINAL_COMMIT_POST_REGENERATION_TEST_OUTPUT.txt` records all 19 original tests passing after generation.
- `ECONOMIC_RESULT_RECONCILIATION.json` proves the correction did not alter family or portfolio metrics.
- `SEGMENT_D_AND_ACCOUNT_EVIDENCE.json` isolates locked-tail, contract and sizing evidence.
- `MULTIREGIME_RUN_MANIFEST.json` binds code, tests, configuration, inputs, outputs and evidence by portable path, byte size and SHA-256.

The approved primary machine classification is `NO_DEFENSIBLE_MULTIREGIME_EDGE`. `MULTIREGIME_V1_ABANDONED_NO_RESCUE` remains only the narrative disposition, and `XAUUSD_1000_ACCOUNT_CONTRACT_GRANULARITY_INADEQUATE` is a secondary stop reason.
