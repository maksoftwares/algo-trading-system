# Adversarial Review Guide

Use this review to actively search for cases where the expert logic fails.

For each losing trade, record:

- whether the entry followed the registered mechanical definition
- whether a logic gap is visible
- whether the loss is an accepted failure mode
- whether the loss suggests curve-fitting pressure

Manual review is `PENDING` until every sampled losing trade has a recorded assessment.
